---
description: Review the project against predefined checklists (e.g., UX, Security, Testing)
---
1. Read the command definition from `.gemini/commands/speckit.checklist.toml`.
2. Parse the `prompt` field from the TOML file.
3. Review the current artifacts against the criteria defined in the project checklists.
4. Report any missing items or violations.
