---
description: Ask clarifying questions to resolve ambiguities in the spec or plan
---
1. Read the command definition from `.gemini/commands/speckit.clarify.toml`.
2. Parse the `prompt` field from the TOML file.
3. Identify ambiguous sections in `spec.md` or `plan.md`.
4. Formulate specific questions for the user to resolve these ambiguities.
