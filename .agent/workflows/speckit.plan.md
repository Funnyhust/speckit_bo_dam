---
description: Create or update the implementation plan (plan.md) based on spec.md
---
1. Read the command definition from `.gemini/commands/speckit.plan.toml` (or similar path in your workspace).
2. Parse the `prompt` field from the TOML file.
3. Follow the instructions to generate or update `plan.md`.
4. Ensure consistency with `spec.md` as required.
