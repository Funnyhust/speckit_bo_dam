---
description: Update or create the software specification (spec.md) using SpecKit
---
1. Read the command definition from `.gemini/commands/speckit.specify.toml`.
2. Parse the `prompt` field from the TOML file.
3. Follow the instructions to interview the user (if needed) or update `spec.md` based on input.
4. Ensure you read the `constitution.md` if required by the command.
