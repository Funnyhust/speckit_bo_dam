---
description: Break down the plan into execution tasks (tasks.md)
---
1. Read the command definition from `.gemini/commands/speckit.tasks.toml` (or similar path in your workspace).
2. Parse the `prompt` field from the TOML file.
3. Follow the instructions to generate `tasks.md` based on `plan.md`.
4. Ensure tasks are atomic and follow the project's task format.
