---
description: Implement the code for a specific task or set of tasks
---
1. Read the command definition from `.gemini/commands/speckit.implement.toml` (or similar path in your workspace).
2. Parse the `prompt` field from the TOML file.
3. Identify the task(s) to be implemented (user may provide Task ID).
4. Follow the instructions to implement the code, verifying against requirements.
