---
description: Validate the project alignment with the core project constitution and principles
---
1. Read the command definition from `.gemini/commands/speckit.constitution.toml`.
2. Parse the `prompt` field from the TOML file.
3. Compare the current spec/plan/code against `constitution.md`.
4. Flag any critical violations of core principles.
